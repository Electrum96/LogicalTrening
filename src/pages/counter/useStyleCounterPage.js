import {StyleSheet} from 'react-native';

function useStyleCounterPage () {
    const styles = StyleSheet.create({
        
    });

    return styles;
}

export default useStyleCounterPage;