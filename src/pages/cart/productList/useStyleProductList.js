import {StyleSheet} from 'react-native';

function useStyleProductList () {
    const styles = StyleSheet.create({
        
    });

    return styles;
}

export default useStyleProductList;